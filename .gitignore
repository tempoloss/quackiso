/target
# tools/mxgen is its own workspace, so its build output lands outside /target.
/tools/mxgen/target
/build
/configure
/venv
*.duckdb_extension
*.duckdb_extension.wasm
.idea
# The venv the verification gates run from; see the Makefile and docs.
/.verify-venv
