/target
# tools/mxgen and tools/mtgen are their own workspaces, so their build output
# lands outside /target.
/tools/mxgen/target
/tools/mtgen/target
/build
/configure
/venv
*.duckdb_extension
*.duckdb_extension.wasm
.idea
# The venv the verification gates run from; see the Makefile and docs.
/.verify-venv
# scripts/test_sweep_foreign_corpora.py is run as `python -m scripts.test_...`,
# which imports the sweep as a package and leaves bytecode beside it.
__pycache__/
# Cloudflare wrangler's local account cache; not this project's state.
/.wrangler
