/target
# tools/mxgen and tools/mtgen are their own workspaces, so their build output
# lands outside /target.
/tools/mxgen/target
/tools/mtgen/target
/build
/configure
/venv
*.duckdb_extension
*.duckdb_extension.wasm
.idea
# The venv the verification gates run from; see the Makefile and docs.
/.verify-venv
